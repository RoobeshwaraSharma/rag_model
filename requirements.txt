# FastAPI and server
fastapi==0.115.0
uvicorn[standard]==0.32.0

# LangChain and related
# langchain-groq 1.0.0 requires langchain-core>=1.0.0
# So we need to use newer langchain packages (>=1.0.0) to work with langchain-groq 1.0.0
# Using compatible versions that work together
langchain>=1.0.0
langchain-community>=0.3.0
langchain-core>=1.0.0
langchain-text-splitters>=0.3.0
langchain-groq==1.0.0
langchain-chroma>=0.1.0
langchain-huggingface>=0.0.3

# Vector store
chromadb==1.3.4

# Embeddings
sentence-transformers==5.1.2

# Data processing
# Versions with pre-built wheels for Python 3.12.4
pandas>=2.0.0,<2.3.0
numpy>=1.24.0,<2.0.0

# Environment variables
python-dotenv==1.0.1

# Pydantic (usually included with FastAPI, but explicit)
pydantic==2.11.10

# Progress bar
tqdm==4.67.1

